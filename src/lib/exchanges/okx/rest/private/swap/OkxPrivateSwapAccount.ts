/**
 * Private swap account endpoints of OKX v5 (instType='SWAP').
 *
 * Static class — never instantiate.
 */
export class OkxPrivateSwapAccount {
  private constructor() {
    throw new Error('OkxPrivateSwapAccount is a static class and cannot be instantiated');
  }
}
