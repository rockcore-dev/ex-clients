/**
 * Private swap trade endpoints of OKX v5 (instType='SWAP').
 *
 * Static class — never instantiate. Методы добавляются итеративно;
 * см. `docs/exchanges/okx/coverage.md`.
 */
export class OkxPrivateSwapTrade {
  private constructor() {
    throw new Error('OkxPrivateSwapTrade is a static class and cannot be instantiated');
  }
}
