/**
 * Private futures trade endpoints of OKX v5 (instType='FUTURES').
 *
 * Static class — never instantiate.
 */
export class OkxPrivateFuturesTrade {
  private constructor() {
    throw new Error('OkxPrivateFuturesTrade is a static class and cannot be instantiated');
  }
}
